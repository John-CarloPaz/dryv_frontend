import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF13005A);
  static const darkBlue = Color(0xFF00337C);
  static const blue = Color(0xFF1C82AD);
  static const accent = Color(0xFF03C988);
}
